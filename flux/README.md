## Structure
```sh
📁 config       # A redirect to bootstrap the configs from components/cluster-vars
```

## App Groups

There are three app groups

- `apps.yaml` -- General apps
- `core.yaml` -- Infrastructure
- `private-apps.yaml` -- Private apps referenced elsewhere
