---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app adguard-primary
  namespace: default
spec:
  interval: 15m
  chartRef:
    kind: OCIRepository
    name: app-template
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      main:
        pod:
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            fsGroup: 1000
            fsGroupChangePolicy: "OnRootMismatch"
            supplementalGroups:
              - 100
          affinity:
            podAntiAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                - labelSelector:
                    matchExpressions:
                      - key: app.kubernetes.io/name
                        operator: In
                        values:
                          - "adguard-secondary"
                  topologyKey: kubernetes.io/hostname
        containers:
          main:
            image:
              repository: mirror.gcr.io/adguard/adguardhome
              tag: v0.107.79

            env:
              PUID: 1000
              GUID: 1000
              TZ: "${CLUSTER_TZ}"

            # CPU from 7d of Prometheus data: p50 3.6m, p95 6.6m, max 10.4m.
            # This is the busier of the two resolvers (it serves the LAN
            # directly). 25m is the practical request floor. No CPU limit, per
            # the cluster convention — a query flood should never be throttled.
            resources:
              requests:
                memory: 10Mi
                cpu: 25m
                ephemeral-storage: 10Mi
              limits:
                memory: 500Mi
                ephemeral-storage: 20Mi

    service:
      main:
        type: LoadBalancer
        controller: main
        annotations:
          lbipam.cilium.io/ips: ${SVC_ADGUARD1_ADDR}                                                                                                                                                                                               │
          lbipam.cilium.io/sharing-key: adguard1
          internal.wynning.tech/hostname: "adguard1.${SECRET_INTERNAL_DOMAIN}"
        externalTrafficPolicy: Local
        ports:
          setup:
            enabled: false
            port: 3000
          http:
            enabled: true
            port: &httpPort 80
            targetPort: 8080
          dns:
            port: 53
            enabled: false
      udp:
        enabled: true
        controller: main
        type: LoadBalancer
        annotations:
          lbipam.cilium.io/ips: ${SVC_ADGUARD1_ADDR}                                                                                                                                                                                               │
          lbipam.cilium.io/sharing-key: adguard1
          tailscale.com/expose: "true"
        externalTrafficPolicy: Local
        ports:
          dns:
            enabled: true
            port: 53
            targetPort: 5053
            protocol: UDP

    route:
      app:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/name: "AdGuard Primary"
          gethomepage.dev/description: "DNS Based Filtering"
          gethomepage.dev/group: "DNS"
          gethomepage.dev/icon: "adguard-home.png"
          gethomepage.dev/weight: "10"
          gethomepage.dev/widget.url: "https://adguard-primary.${SECRET_INTERNAL_DOMAIN}"
          gethomepage.dev/widget.type: "adguard"
          internal.wynning.tech/hostname: &host "{{ .Release.Name }}.${SECRET_INTERNAL_DOMAIN}"
        hostnames:
          - *host
        parentRefs:
          - name: envoy-internal
            namespace: networking
        rules:
          - backendRefs:
              - name: adguard-primary-main
                port: *httpPort

    persistence:
      config:
        enabled: true
        existingClaim: adguard-config
        globalMounts:
          - path: /opt/adguardhome/conf
      work:
        enabled: true
        existingClaim: adguard-work
        globalMounts:
          - path: /opt/adguardhome/work
  valuesFrom:
    - kind: Secret
      name: adguard-sync
      valuesKey: USERNAME
      targetPath: route.app.annotations.gethomepage\.dev/widget\.username
    - kind: Secret
      name: adguard-sync
      valuesKey: PASSWORD
      targetPath: route.app.annotations.gethomepage\.dev/widget\.password
